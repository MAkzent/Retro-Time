# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CCC' do
  use_frameworks!

  # Pods for CCC
    pod 'Firebase/Core'
    pod 'Firebase/Database'

end
